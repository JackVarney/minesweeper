import { cleanUpZeroes } from '../';

describe('cleanUpZeroes', () => {
  it('should clean up all non blocked zeroes', () => {
    const point = {
      x: 1,
      y: 1,
    };

    const tiles = [
      [
        {
          point: {
            x: 0,
            y: 0,
          },
          hasBeenSweeped: false,
        },
      ],
    ];
  });
});
